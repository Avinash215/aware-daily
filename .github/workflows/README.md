# Deploy workflow

This workflow builds the Vite app and deploys `dist/` to GitHub Pages on every push to `main`, or when manually started.

## Re-run

In GitHub: **Actions** → **Deploy to GitHub Pages** → select a run → **Re-run jobs**.
You can also start a fresh run with **Run workflow**.

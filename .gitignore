# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Staged at run time by the pipeline's entrypoint.py (the novelty ledger it
# publishes as a static asset). Vite copies it into dist/ during the build; it
# is never committed, because a checked-in copy would be stale the next day.
public/state/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
